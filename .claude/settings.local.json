{
  "permissions": {
    "allow": [
      "Bash(npx tsc *)",
      "Bash(npx next *)",
      "Bash(node -e \"const ts = require\\('typescript'\\); const cfg = ts.readConfigFile\\('tsconfig.json', ts.sys.readFile\\); const parsed = ts.parseJsonConfigFileContent\\(cfg.config, ts.sys, '.'\\); const prog = ts.createProgram\\(parsed.fileNames, parsed.options\\); const diags = ts.getPreEmitDiagnostics\\(prog\\); diags.slice\\(0,10\\).forEach\\(d => { const f = d.file?.fileName; const l = d.file ? ts.getLineAndCharacterOfPosition\\(d.file, d.start||0\\) : null; console.log\\(f + \\(l ? ':' + \\(l.line+1\\) : ''\\) + ' - ' + ts.flattenDiagnosticMessageText\\(d.messageText, ' '\\)\\); }\\); console.log\\('Total errors:', diags.length\\);\")",
      "Bash(node_modules/.bin/tsc --noEmit)"
    ]
  }
}
